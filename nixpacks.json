{
  "providers": ["python"],
  "phases": {
    "setup": {
      "nixPkgs": ["python311", "pip"]
    },
    "install": {
      "cmds": ["pip install -r backend/requirements.txt"]
    },
    "build": {
      "cmds": []
    },
    "start": {
      "cmd": "cd backend && python server.py"
    }
  }
}